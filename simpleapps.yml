kind: ExternalService
name: simpleapps
spec:
    image: hesam80/simpleappss:latest
    image_pull_policy: Always
    image_pull_secret: dockerhub
    port: 5000
    replicas: 1
    resources:
        memory: 200Mi

